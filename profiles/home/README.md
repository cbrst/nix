# Home Manager Profiles

These profiles group portable user modules into roles. They work in standalone
Home Manager configurations on Linux and macOS and in Home Manager embedded in
NixOS.

Import a profile from a standalone file under `hosts/home/`, or from the
`home-manager.users.<name>` section of a NixOS host.
