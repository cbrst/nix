diff --git a/grammar.js b/grammar.js
index c50bbd8..f29277e 100644
--- a/grammar.js
+++ b/grammar.js
@@ -93,7 +93,7 @@ module.exports = grammar({
     include_statement: ($) =>
       seq(
         "@include",
-        $.identifier,
+        choice($.identifier, alias($.qualified_identifier, $.identifier)),
         optional(alias($.include_arguments, $.arguments)),
         choice($.block, ";")
       ),
@@ -348,7 +348,11 @@ module.exports = grammar({
 
     unit: ($) => token.immediate(/[a-zA-Z%]+/),
 
-    call_expression: ($) => seq(alias($.identifier, $.function_name), $.arguments),
+    call_expression: ($) =>
+      seq(
+        alias(choice($.identifier, $.qualified_identifier), $.function_name),
+        $.arguments
+      ),
 
     binary_expression: ($) =>
       prec.left(
@@ -360,6 +364,9 @@ module.exports = grammar({
     identifier: ($) =>
       /((#\{[a-zA-Z0-9-_,&\$\.\(\) ]*\})|(--|-?[a-zA-Z_]))([a-zA-Z0-9-_]|(#\{[a-zA-Z0-9-_,&\$\.\(\) ]*\}))*/,
 
+    qualified_identifier: ($) =>
+      token(prec(1, /[a-zA-Z_-][a-zA-Z0-9_-]*\.[a-zA-Z_-][a-zA-Z0-9_-]*/)),
+
     variable_identifier: ($) => /([a-zA-Z_]+\.)?\$[a-zA-Z-_][a-zA-Z0-9-_]*/,
 
     at_keyword: ($) => /@[a-zA-Z-_]+/,
