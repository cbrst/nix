# vim: set ft=zsh:

function main() {
	local command=${1}
	shift

	case "$command" in
	export | unset)
		fzf --preview "eval 'echo $'{}" "$@"
		;;
	ssh)
		fzf --preview "(doggo {} 2> /dev/null || dig {})" "$@"
		;;
	*)
		fzf --preview "fzf-preview {}" "$@"
		;;
	esac
}

main "$@"
